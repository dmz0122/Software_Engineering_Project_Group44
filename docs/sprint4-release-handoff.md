# Sprint 4 Release Handoff

This sprint closes the analytics and admin-tooling backlog for Group 44.

## Main Deliverables

- Data seeding and activity log support
- Service-layer logging and AI client wiring
- Analytics engine and coverage for the new insight flows
- Admin analytics workspace
- Applicant insight workspace

## Before Demo

- Confirm `llm.properties` matches the intended runtime mode
- Start the app with `mvn exec:java`
- Sign in with the seeded applicant and admin accounts
- Verify export, logs, and analytics views load against the seeded CSV data
